*.log
.DS_Store
# Qualquer variante de .env fica de fora — a lista a mao (.env.backup,
# .env.production) nao previa nomes novos, e um `.env.antes-do-smtp` criado
# antes de mexer em producao acabou barrando o proprio deploy pela guarda de
# arvore limpa do deploy.sh (etapa 11). A guarda esta certa; a lista e que
# era curta demais.
.env
.env.*
!.env.example
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# Etapa 13: package.json proprio do monitor de mudancas, em monitor/.
/monitor/node_modules
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Gerado por `php artisan comparador:gerar-json` (regra 9).
/public/dados

# /public/build NAO entra aqui: e versionado de proposito (etapa 11).
# O servidor da Hostinger nao tem Node, e o plugin de fontes do Vite baixa
# as familias da Bunny durante o build — compilar em producao poria o CDN
# de terceiro no caminho critico do deploy. Rode `npm run build` e commite
# o resultado junto da mudanca de front-end.

// Pagina de marca: so o Alpine, para os seletores da tabela de taxas (plano,
// prazo e bandeira). Sem o motor e sem o catalogo do comparador — a tabela vem
// pronta do servidor e o Alpine apenas filtra as linhas.
import Alpine from 'alpinejs';

window.Alpine = Alpine;
Alpine.start();
